{
  "name": "ate-bit",
  "version": "0.1.0",
  "description": "Terminal experiment with a snake game",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "keywords": ["terminal", "games", "ascii", "web", "cli", "retro"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
